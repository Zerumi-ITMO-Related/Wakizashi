fun factorial(n : Int) : Int {
    if (n == 1) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }

    return 0;
}

fun main() : Unit {
    print_number(factorial(10));
    return Unit;
}
