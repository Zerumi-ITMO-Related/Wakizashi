fun factorial(n : Int) : Int {
    if (n == 1) {
        return 1;
    } else {
        val fac: Int = n * factorial(n - 1);
        return fac;
    }

    return 0;
}

fun main() : Unit {
    val fac_res: Int = factorial(4);

    print_number(fac_res);

    return Unit;
}
